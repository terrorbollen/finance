# Agent Coordination

Multi-agent task board and improvement log. Every agent reads this before starting and updates it when finished — or whenever they spot something worth doing.

> Full contribution guidelines (coding standards, testing rules, scope discipline): [`CONTRIBUTIONS.md`](../CONTRIBUTIONS.md)
> Backtest validation rules (what makes a result trustworthy, horizon guide, known limitations): [`backtesting/STRATEGY.md`](STRATEGY.md)

## Protocol

### Before you start
1. Read this file, `CLAUDE.md`, and `CONTRIBUTIONS.md`.
2. Run `ls .agent-locks/` to see which tasks are currently claimed.
3. Pick the **top open task** that has no lockfile.
4. **Claim it atomically:** `mkdir .agent-locks/TASK_ID` — if this fails, someone else just claimed it; pick another task.
5. Update **Claimed by** and **Status** to `in_progress` in the task table below.

### While you work
6. Touch only files listed in your task's **Scope**.
7. If you notice a bug or improvement **outside your scope**, add it as a new `open` task — don't fix it now.

### Before you finish
8. Run `uv run ruff check .` and `uv run mypy .` — both must pass clean.
9. Run `uv run pytest` — all tests pass.
10. If your task touches `models/`, `data/features.py`, or `signals/`, run a backtest before and after.
11. Set **Status** to `done` and clear **Claimed by** in the task table.
12. Release the lock: `rmdir .agent-locks/TASK_ID`
13. Add an entry to `CHANGELOG.md` — what changed and why, not just which files.

> **Priority:** Tasks are listed top-to-bottom within each section from highest to lowest priority.

---

## Task Board

### Backtesting (`backtesting/`)

| # | Task | Scope | Claimed by | Status |
|---|------|-------|------------|--------|
| B19 | **Monte Carlo simulation was showing order-independent return column — replaced with max drawdown.** The return column always showed identical p5/mean/p95 because compounded return is commutative. Fixed by replacing the return column in the display with max drawdown (genuinely order-dependent) and adding `observed_max_drawdown_pct` to `MonteCarloResult`. Sharpe CI was already working correctly. | `backtesting/metrics.py`, `backtesting/results.py` | — | done |
| B20 | **Backtest summary has no temporal breakdown — concentration risk is invisible.** `BacktestResult.summary()` only reports aggregate metrics (total net return, overall win rate, Sharpe). A result like "Sharpe 3.3, win rate 94%" can be entirely driven by a single quarter while the other three quarters produce zero trades — exactly what we observed in Q2 2025 (tariff shock). Fix: add a quarterly breakdown section to `BacktestResult.summary()` that groups `daily_predictions` by calendar quarter and prints per-quarter trade count, win rate, and net return. Flag quarters with 0 trades explicitly. Add `--breakdown monthly` flag to `cmd_backtest` in `main.py` to switch from quarterly to monthly grouping for higher-frequency models. No changes needed outside these two files — `daily_predictions` is already populated. | `backtesting/results.py`, `main.py` | — | open |
| B21 | **No multi-ticker backtest command — generalisation across tickers requires manual runs and mental aggregation.** Currently `main.py backtest` accepts a single ticker. To evaluate the model across all tickers in a config, the user must run 3–10 separate commands, manually compare numbers, and apply the STRATEGY.md trust criteria in their head. Fix: add a `backtest-all` subcommand to `main.py` that reads `tickers` from `--config`, calls `backtester.run()` per ticker sequentially, and prints a single summary table: ticker, trade count (with ⚠ if < 30), win rate (with `*`/`**` significance markers), net return (with ✗ if negative), Sharpe, max drawdown. The table applies the STRATEGY.md criteria automatically so the user can immediately see which tickers pass all checks. | `main.py` | — | open |
| B22 | **No direct comparison between walk-forward and standard training — it is impossible to know if walk-forward is worth the compute cost.** After a walk-forward run, there is no automated way to compare it against the standard-trained model checkpoint on the same holdout period and tickers. Without this, every walk-forward run is evaluated in isolation and any improvement could be noise. Fix: add a `--compare-checkpoints` flag to `cmd_backtest` (or a new `compare` subcommand) that accepts two model `--name` values, runs the same backtest with each, and prints a side-by-side table: net return, Sharpe, Sortino, win rate, max drawdown, trade count. This makes the walk-forward vs standard decision data-driven. | `main.py`, `backtesting/backtester.py` | — | open |
| B24 | **`_bootstrap_ci()` in `metrics.py` hand-rolls bootstrap resampling that `scipy.stats.bootstrap()` already provides.** The method at lines 478–491 manually draws `n_boot=500` integer-index samples, calls a stat function on each resample, sorts results, and slices p2.5/p97.5 — exactly the percentile bootstrap that `scipy.stats.bootstrap()` implements. scipy is already imported at line 9. The custom version lacks bias correction and uses a fixed resampling size equal to `n`, whereas scipy's implementation supports BCa (bias-corrected and accelerated) intervals which are more accurate for skewed distributions like Sharpe ratios. Fix: replace `_bootstrap_ci()` with a thin wrapper around `scipy.stats.bootstrap((arr,), stat_fn, n_resamples=500, confidence_level=0.95, method='percentile', random_state=42)` and read `.confidence_interval.low` / `.high`. The three call sites at lines 419–424 pass different `stat_fn` lambdas — verify each is compatible with scipy's `(data,) -> scalar` signature before replacing. | `backtesting/metrics.py` | — | open |
| B23 | **`_bh_correction()` in `backtester.py` manually reimplements Benjamini-Hochberg FDR correction that scipy already provides.** The function at lines 23–35 hand-rolls the BH algorithm including sorted ranking, correction factor, and backward monotonicity enforcement — exactly what `scipy.stats.false_discovery_control()` does (available since scipy 1.8, which is already a transitive dependency). The custom code is correct but adds maintenance surface and diverges from a well-tested reference implementation. Fix: remove `_bh_correction()` and replace the single call site at line 425 with `list(scipy.stats.false_discovery_control(raw_pvalues))`. Note that scipy's function accepts an array and returns an array, so a `list()` cast is needed to preserve the return type. | `backtesting/backtester.py` | — | open |

### Risk Management (`signals/`)

| # | Task | Scope | Claimed by | Status |
|---|------|-------|------------|--------|

### Feature Engineering (`data/`)

| # | Task | Scope | Claimed by | Status |
|---|------|-------|------------|--------|

### Model Improvements (`models/`)

| # | Task | Scope | Claimed by | Status |
|---|------|-------|------------|--------|
| M6 | **Multi-ticker normalization may leak across ticker boundaries.** In `training.py`, all tickers are concatenated into a single feature array before the train/val/test split. The normalization mean/std is then computed on rows `0 : train_end_raw`. If tickers have different date ranges (likely — some have longer history than others), the rows from later-dated tickers may land in the "training" portion alongside early rows from other tickers, or vice versa. This isn't a pure temporal split. Fix by computing normalization stats per ticker on that ticker's training portion only, then applying them before concatenation. This preserves the invariant that no future data informs normalization. | `models/training.py` | — | done |
| M7 | **Off-by-one in max-return label creation.** In `training.py`, the sliding window for max-return labels is created from `close_vals[1:]` (one bar shifted forward), but returns are divided by `close_vals[:n-h]` (starting from bar 0). This means the max return over window [t+1 … t+h] is divided by the price at t−1 instead of t, misstating every label by one day. Needs a unit test to confirm, then align indices so numerator window and denominator price both reference bar t. | — | done |
| M8 | **No feature-dimension check before stacking multi-ticker arrays.** After `prepare_data()` is called per ticker, the feature arrays are appended to `all_features` without asserting `features.shape[1]` is consistent across tickers. If the feature engineer drops different columns for one ticker (e.g. missing volume on an index), `np.vstack(all_features)` crashes with a cryptic shape error after potentially minutes of data fetching. Fix: assert `features.shape[1] == expected_n_features` immediately after each `prepare_data()` call, naming the mismatching ticker. | `models/training.py` | — | done |
| M9 | **Walk-forward window results have no calendar dates — regime failures cannot be diagnosed.** `WindowResult` stores only integer indices (`val_start_idx`, `val_end_idx`) into the concatenated feature array. When a window shows `val_accuracy=0.38`, there is no way to know whether that failure occurred in a 2022 bear market, a 2023 ranging period, or a 2024 bull run — making it impossible to correlate poor windows with known market events. Fix: during `run()`, track the date index from the raw DataFrames before `vstack` and store `val_start_date: date | None` and `val_end_date: date | None` on `WindowResult`. Print dates alongside window IDs in `WalkForwardResult.summary()`. Add a note that dates reflect concatenated ticker order, not a single continuous period, so multi-ticker date ranges are approximate. | `models/walk_forward.py` | — | open |
| M10 | **Walk-forward summary reports label distribution, not prediction distribution — HOLD-collapse is invisible.** `WindowResult.class_distribution` stores what fraction of *labels* are BUY/SELL/HOLD. A model that predicts HOLD for every input will match the label distribution and appear accurate — but makes zero actionable trades. This is the most common silent failure mode for LSTM signal models. Fix: after `model.model.evaluate()` in `train_window()`, call `model.model.predict(X_val)` → argmax → compute BUY recall and SELL recall against the true labels. Store `buy_recall: float` and `sell_recall: float` on `WindowResult`. In `summary()`, print a single line: "Active windows (BUY recall > 0.3 AND SELL recall > 0.3): X / Y" and flag collapsed windows with a ✗ marker. Export full per-class F1 per window to a CSV when `--output` is passed. | `models/walk_forward.py` | — | open |
| M11 | **Walk-forward has no trading-quality proxy per window — val_accuracy cannot distinguish a useful model from a HOLD-biased one.** Even with HOLD-collapse detection (M10), accuracy alone cannot tell you if correct predictions correspond to profitable direction calls. `y_price` (actual forward returns) is already computed for every row. Fix: in `train_window()`, after getting predictions, compute a simulated win rate: BUY predictions with positive forward return = win, SELL predictions with negative forward return = win, HOLD predictions excluded. Store `simulated_trade_count: int` and `simulated_win_rate: float | None` (None when trade count < 5) on `WindowResult`. Print both in `summary()` alongside val_accuracy. This directly answers "of the trades this window's model would have taken, what fraction were correct?" and is directly comparable to the win rate in a full backtest. Requires M9 (dates) to be interpretable by regime. | `models/walk_forward.py` | — | open |

### Signal Logic (`signals/`)

| # | Task | Scope | Claimed by | Status |
|---|------|-------|------------|--------|
| S4 | **Signal timestamp uses wall-clock time instead of data date.** `Signal.timestamp` is set to `pd.Timestamp.now()` at generation time regardless of which date's data produced the signal. When generating signals for historical data (backtest replay, debugging, calibration), every signal is stamped with today's date. This breaks audit trails and makes it impossible to reconstruct when a signal was valid. Fix: pass the last date from the feature DataFrame and use it as the timestamp; fall back to `now()` only when no date context is available. | `signals/generator.py` | — | done |
| S1 | Signal filtering (skip earnings, low-volume, low-confidence) | `signals/generator.py` | — | done |
| S2 | **ADX regime filter — suppress directional signals in ranging markets.** Added `min_adx: float \| None` to `SignalGenerator.__init__()` and `Backtester.__init__()`. When set, any BUY or SELL signal is downgraded to HOLD if the latest ADX(14) is below the threshold (or NaN). ADX value is read from `adx_14` in the feature DataFrame (generator) or from the pre-computed per-day `adx` variable (backtester). CLI flag `--min-adx` added to `cmd_backtest`. Default is `None` (disabled). | `signals/generator.py`, `backtesting/backtester.py`, `main.py` | — | done |
| S3 | Multi-timeframe confirmation (daily confirmed by weekly) | `signals/generator.py`, `data/fetcher.py` | — | done |
| S5 | **`scan()` does not increment open position count — `max_positions` limit is never enforced across a scan.** `SignalGenerator.scan()` calls `generate()` for each ticker with the same `open_position_count` value throughout the loop. If `max_positions=3` and the caller passes `open_position_count=0`, all tickers receive the same count, meaning the 4th, 5th, and 6th signals are generated without knowing that three positions are already being entered in this same scan. In a real portfolio scan, this silently bypasses the position limit. Fix: increment a local counter inside `scan()` each time a non-HOLD signal is returned, and pass the updated count to subsequent `generate()` calls. | `signals/generator.py` | — | open |
| S6 | **HOLD signals display a stop-loss and target price that imply a trade is happening.** In `generate()`, when `direction == Direction.HOLD`, `stop_loss` is set to `current_price * (1 - stop_distance)` and `target_price` is set to `current_price * (1 + predicted_change / 100)`. These values are printed in `Signal.__str__()` under "Stop Loss" and "Target Price", suggesting the model is targeting a move when it has explicitly said to do nothing. Fix: for HOLD signals, set `stop_loss = current_price` and `target_price = current_price` (or omit them from the display entirely) so the output accurately reflects the no-trade recommendation. | `signals/generator.py` | — | open |
| S7 | **Two separate Kelly implementations exist with different formula parameterisations — live signals and backtests can silently diverge.** `SignalGenerator._kelly_position_size()` (signals/generator.py) computes Kelly using `confidence_pct / 100` as win probability and `predicted_change / stop_distance` as the reward-to-risk ratio. `PortfolioBacktester._kelly_fraction()` (backtesting/portfolio.py) uses a different parameterisation. Neither calls the other. If one is updated, the other silently produces different position sizes, making it impossible to trust that a signal's stated `position_size` matches what the backtester would have sized. Fix: extract a single `kelly_fraction(p, b)` function into a shared utility module (e.g. `models/kelly.py`), and have both callers use it. | `signals/generator.py`, `backtesting/portfolio.py` | — | open |
| S8 | **Calibration bucket lookup matches both adjacent buckets at boundary values — confidence exactly on a bucket edge returns the wrong calibration.** In `ConfidenceCalibrator.calibrate()`, every bucket is inclusive on both ends (`raw_min <= raw_confidence <= raw_max`). A value sitting exactly on the shared edge between two buckets (e.g. 0.30 matches both the 0.20–0.30 bucket and the 0.30–0.40 bucket) returns the first match — the lower bucket's calibration. This is deterministic but wrong for boundary values. Fix: use half-open intervals `[raw_min, raw_max)` for all buckets except the last (which remains fully closed), matching the logic already applied to the last bucket in `fit()` (line 79–81). | `signals/calibration.py` | — | open |

### Execution Strategy (`signals/`, `backtesting/`)

| # | Task | Scope | Claimed by | Status |
|---|------|-------|------------|--------|
| E1 | Time-based exits (exit if signal stale after N days) | `backtesting/backtester.py` | — | open |
| E2 | Trailing stops (based on recent highs/lows) | `signals/generator.py` | — | open |
| E3 | Partial exits (scale out as price target approaches) | `signals/generator.py`, `backtesting/backtester.py` | — | open |

### Infrastructure (`main.py`, new files)

| # | Task | Scope | Claimed by | Status |
|---|------|-------|------------|--------|
| I3 | Automated retraining: schedule periodic model retraining with new data | `models/training.py`, `main.py` | — | open |
| I4 | Real-time alerts: notify on high-confidence signals above threshold | `main.py`, `signals/generator.py` | — | open |
| I5 | Add type annotations to all CLI command handlers in main.py (`args: argparse.Namespace`, `-> None`) | `main.py` | — | open |
| I6 | Enable `--check-untyped-defs` in mypy config (currently inner function bodies like `_do_training` in training.py are not type-checked at all) | `pyproject.toml` or `mypy.ini` | — | open |
| I7 | Tighten return types in mlflow_tracking.py: `get_recent_runs` returns `list[dict]`, `get_best_run` returns `dict \| None` — both should use `dict[str, Any]` | `models/mlflow_tracking.py` | — | open |
| I8 | Print `holdout_start_date` prominently after training so users immediately see where the holdout boundary is before running a backtest | `main.py` | — | open |
| I9 | **Remove fallback normalization — it introduces data leakage.** When `signal_model_config.json` is missing or can't be loaded, both `backtester.py` and `generator.py` fall back to computing normalization mean/std from the current data window. This is data leakage: for a prediction on day T, the window includes days T-seq_len through T, so the normalization is influenced by the most recent prices — prices the model is supposed to be predicting without knowing. INVARIANTS.md already forbids this fallback in production, but there's no enforcement. Replace the fallback with a hard `raise RuntimeError` and a clear message telling the user to retrain and save the config. The fallback may also hide the case where someone forgets to run `train` before `backtest`. | `backtesting/backtester.py`, `signals/generator.py` | — | open |
| I12 | **No tests for `portfolio.py`, `data/fetcher.py`, or `models/losses.py`.** These three modules have zero test coverage: `portfolio.py` contains the Kelly sizing bug (P1) and commission double-counting (P2) that need regression tests before fixing; `fetcher.py` has the cross-asset reference data fallback that is a known invariant concern; `losses.py` has focal-loss implementations used in every training run. Add `tests/test_portfolio.py` (Kelly sizing, commission, P1/P2 regression), `tests/test_fetcher.py` (fallback behaviour when yfinance returns no data), and `tests/test_losses.py` (focal loss output shape, class-weight exclusivity). | `tests/` | — | done |
| I10 | Backfill `holdout_start_date` in old model configs or add a CLI flag (`--holdout-start`) to pass it at backtest time so holdout enforcement works on pre-existing checkpoints | `main.py`, `backtesting/backtester.py` | — | open |
| I11 | Log `leverage` to MLflow in backtest runs: `cmd_backtest` logs ticker/dates/commission but omits `leverage`, making it impossible to distinguish or compare leveraged runs in history | `main.py` | — | open |
| I13 | **`models/signal_model.py` is 24% covered — `predict()` is entirely untested.** `SignalModel.predict()` returns a per-horizon dict of `(signal, confidence, price_change)` tuples and a consensus signal. None of this is exercised in the test suite. A shape mismatch, wrong horizon key, or swapped BUY/SELL index would corrupt every signal and backtest silently. Add `tests/test_signal_model.py` covering: (1) `predict()` returns one key per trained horizon with correct shape; (2) confidence is in [0, 1]; (3) consensus signal matches the majority of per-horizon signals on a synthetic input; (4) `EnsembleSignalModel.predict()` averages correctly when the two backbones disagree. Use a tiny model (1 layer, small dims) built in-process — do not load weights from disk. | `tests/test_signal_model.py` | — | open |
| I14 | **`signals/generator.py` stop-loss, Kelly sizing, and confidence-filter paths are untested (57% coverage).** The ATR stop-loss calculation (lines 138–170), Kelly position sizing (lines 174–215), and confidence threshold filtering (lines 238–270) are not exercised. These paths directly determine the entry price, stop-loss level, position size, and whether a signal is suppressed — all values shown to the user. Add tests to `tests/test_signal_generation.py`: (1) ATR stop-loss distance equals `atr * multiplier / 100 * price`; (2) Kelly fraction is capped at `max_position`; (3) a signal below `min_confidence` returns `None`; (4) OOD feature warning fires when a feature exceeds 3 std. Use a `MockModel` that returns fixed per-horizon outputs. | `tests/test_signal_generation.py` | — | open |
| I15 | **`models/walk_forward.py` (30%) and `backtesting/results.py` (52%) have large untested paths.** In `walk_forward.py`, the window-generation logic (purge/embargo gap, fold count) and the assertion that raises on zero windows are never exercised by tests. In `backtesting/results.py`, the CSV export, JSON summary, and benchmark return columns (lines 278–426) have no coverage. Add: (1) `tests/test_walk_forward.py` — verify correct window count for given train/val sizes, purge, and embargo; assert zero-window error is raised with the right message; (2) extend `tests/test_metrics.py` or add `tests/test_results.py` — round-trip a `BacktestResults` object through CSV export and re-import, verify equity curve length and benchmark column presence. | `tests/test_walk_forward.py`, `tests/test_results.py` | — | open |
| I16 | **`tests/test_portfolio.py` line 216 has a mypy type error that will crash at runtime.** A variable typed as `int \| None` is used as the right-hand operand of `/` without a `None`-guard. `mypy` reports `Unsupported operand types for / ("float" and "None")`. If the `None` branch is reached, Python raises `TypeError` and the test (and any code path it exercises) crashes silently in CI. Fix: add a `assert variable is not None` or an `if variable is None` guard before the division, or narrow the type at the point of assignment. | `tests/test_portfolio.py` | — | done |
| I17 | **No MLflow run URL is printed after `train`, `backtest`, `portfolio`, or `walk-forward` — users cannot inspect the run without manually navigating MLflow.** In `cmd_backtest`, only `"Results logged to MLflow."` is printed; the run URL is never constructed. In `cmd_train` (standard), the link comes from Keras autolog side-effects, not from `main.py`, and disappears if autolog is disabled. In `cmd_train` (walk-forward), per-window links are printed by the trainer but no final parent-run URL is shown. In `cmd_portfolio`, there is no MLflow logging at all. Fix: after each command's MLflow context closes, retrieve the run ID via `mlflow.last_active_run()` and construct the UI URL as `{tracking_uri}/#/experiments/{experiment_id}/runs/{run_id}`, then print it. For walk-forward, print the parent run URL once at the end. For portfolio, add a minimal MLflow run logging result-level metrics and print its URL. | `main.py` | — | open |
| I18 | **No test asserts that inference normalization uses config stats rather than live-window stats.** `SignalGenerator._load_config()` loads `feature_mean` and `feature_std` from the saved `signal_model_config.json` and uses them to normalize features at inference time — correctly following INVARIANTS.md §1. However, `tests/test_signal_generation.py` only tests the fallback path (when `feature_mean=None`) and never asserts that config-sourced stats are actually applied to the feature array. If the config-loading path silently broke (e.g. wrong key name, Pydantic field rename, or assignment bug), inference would fall back to live-window stats and produce degraded signals with no test failing. Fix: add a test that constructs a `SignalGenerator` with a mock config containing known `feature_mean`/`feature_std` values, calls `generate()`, and asserts the normalized feature array matches `(raw - config_mean) / config_std` rather than window-computed stats. | `tests/test_signal_generation.py` | — | open |

### Portfolio (`backtesting/`)

| # | Task | Scope | Claimed by | Status |
|---|------|-------|------------|--------|
| P1 | **Kelly position sizing uses initial capital instead of current capital.** `position_capital = min(capital, self.initial_capital * kelly_f)` — the cap is current capital but the sizing basis is `initial_capital`. After a drawdown, a 50% Kelly fraction can still attempt a $5k position even if only $2k cash remains, potentially allocating more than 100% of available capital. Fix: use `current_capital * kelly_f` as the sizing basis. | `backtesting/portfolio.py` | — | done |
| P2 | **Portfolio commission double-counts leverage.** Commission is computed as `entry_val * commission_pct * 2 * eff_lev`. If `entry_val` is the base cash committed (pre-leverage), multiplying by `eff_lev` is correct. But if it's already the leveraged notional, this inflates costs by `eff_lev`. Audit which convention `entry_val` follows and make commission calculation consistent with it. Incorrect commission math makes leveraged backtests report unrealistically bad net returns. | `backtesting/portfolio.py` | — | done |

---

> Active locks: run `ls .agent-locks/` to see which tasks are currently in progress.
